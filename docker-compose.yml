version: '3'
services:
  web:
    image: chessbuddy
    build:
      context: .
      dockerfile: Dockerfile
    ports:
      - "7800:7800"
    expose:
      - 7800
    restart: always
    environment:
      - SERVER_ADDRESS=0.0.0.0
    networks:
      - chessbuddy
networks:
  chessbuddy:

